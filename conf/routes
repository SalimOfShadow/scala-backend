# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
GET     /                           controllers.HomeController.index()

+nocsrf
POST    /create-user                controllers.UserController.createUser()

# TEST - Sessions
GET    /test/getSession                  controllers.TestController.testSession()
GET    /test/getAllSession               controllers.TestController.testAllSession()
GET    /test/insertSession               controllers.TestController.testConnection()
GET    /test/print                       controllers.TestController.testPrintSecrets()

# TEST - JWT
GET    /test/createToken                 controllers.TestController.testTokenCreationAndExpiry()
GET    /test/validateToken               controllers.TestController.testTokenValidation()




# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
